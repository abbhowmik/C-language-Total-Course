#include <stdio.h>
#include <math.h>

int main()
{
    int side;
    printf("Enter the value of side\n");
    scanf("%d", &side);
    printf("The value of area is %0.1f unti^2", pow(side, 2)); // it basically does squaere of the enter value

    return 0;
}